package com.java.topis;

/**
 * Examples of the hash function.
 1. for large keys :- h(key) = key % m   (where: m is any prime number)
 2. for string keys :- weighted Sum
    => (s[0]*x^0 + s[1]*x^1 + s[2]*x^2 + s[3]*x^3) % m
 3. for object key :- weighted Sum of elements

**/


public class InternalHashMap {

    public static void main(String[] args) {

    }
}